<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.soft.gift.mapper.OrderDAO">
	<!-- <insert id="insertOrder" parameterType="com.gift.model.Order" keyProperty="id">
		INSERT INTO `t_order` (`account`,`status`,`address_id`,`total_price`,`type`) VALUES(#{order.account},#{order.status},#{order.address_id},#{order.total_price},#{order.type})
	</insert> -->
</mapper>